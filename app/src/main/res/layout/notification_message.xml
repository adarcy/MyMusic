<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:gravity="center_vertical"
    android:paddingRight="@dimen/play_controller_margin"
    android:paddingLeft="@dimen/play_controller_margin">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="@dimen/notification_message_height"
        android:layout_height="@dimen/notification_message_height"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/button_padding"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="这是歌曲名称"
            android:textColor="@color/text" />

        <TextView
            android:layout_marginTop="@dimen/item_icon_padding"
            android:textSize="10sp"
            android:id="@+id/tv_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/text_grey"
            tools:text="这是歌曲名称" />
    </LinearLayout>

</LinearLayout>

